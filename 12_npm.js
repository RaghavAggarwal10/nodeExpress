//npm -global command,comes with node 
//npm --version

//local dependency - use it only in this particular project
// npm i <packageName>

//global dependency - use it in any project 
//npm install/i -g <packageName>

//package.json  - manifest file(stores important info about project/package)
//manuel approach(create package.json in the root,create properties etc )
//automatic approach 

//npm init (step by step ,press enter to skip )
//when we run it the default package name is the name of the folder but we can change it to 
//which ever file we download is present in dependency
//npm install to install all the dependency that a person used dring the project and also never send node modules in github  
//npm init -y(everything default)